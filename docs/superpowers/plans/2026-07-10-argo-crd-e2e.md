# 真 Argo CRD e2e Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 kind 装真 Argo Workflow，把 workflow-svc 从 `argo_mode=stub` 切到 `k8s`，端到端验证 `K8sArgoClient` 全部能力（submit/get_status/get_steps/cancel/resume/logs SSE）+ MinIO 产物往返。

**Architecture:** Argo 走官方 `install.yaml` 装 `argo` ns（ClusterRole watch 全 ns）；workflow-svc 用 in-cluster SA token 打 CRD；step Pod 用独立 `argo-exec` SA（pods RBAC）；`argo_mode` 由 kind overlay ConfigMap patch 切 k8s（base 仍 stub）；dispatcher 补 cancel/resume/logs 薄代理让 e2e 全走网关；MinIO 产物走 Argo 原生 `artifactRepository`（不写 apihub-core S3 client）。

**Tech Stack:** Python 3.11 / FastAPI / httpx（K8sArgoClient）/ pydantic / asyncpg / Argo Workflows（manifest）/ kind / Kustomize / MinIO / busybox。

## Global Constraints

（每个 task 的要求隐式包含本节；值逐字来自 spec/CLAUDE.md/handoff）

- **DB**：asyncpg 直连（**非** SQLAlchemy）；PG RLS 多租户；superuser `apihub`/`apihub_dev_pwd`，业务账号 `apihub_app`/`apihub_app_dev_pwd`（NOSUPERUSER）。
- **ID 类型**：`workflow_instance.id` 是 **int**（bigserial）；`api_id`/`app_id`/`tenant_id` 是 **str**。
- **namespace**：Argo Workflow CRD 跑 `apihub-workflow`（**单数**）。
- **jsonb**：asyncpg 池已注册 jsonb codec → dict 直传，**别** `json.dumps`+`::text`+`json.loads`。
- **ruff**：CI 用 `ruff==0.6.*`，**check + format 都查**；本地代理挂时装不了 `uvx ruff==0.6.9` 时用 `.venv-t1/bin/ruff`（0.15.20 近似，**CI 以 0.6.9 为准**）。
- **pytest**：`asyncio_mode=auto`；本地 `.venv-t1/bin/pytest`（9.1.1）。单测命令写 `pytest ...`，挂了用 `.venv-t1/bin/pytest`。
- **OTel**：`opentelemetry-sdk==1.40.0` + `opentelemetry-instrumentation-fastapi==0.61b0`（配对，别漂移）。
- **kind**：集群 `kind-apihub`；APISIX NodePort `:30080`；demo key `ak_test_a_demo001`（tenant_a/app_trading）。
- **代理坑**（handoff §45）：`https_proxy=http://127.0.0.1:12348` 转发外网全挂；外网命令前缀 `env -u HTTPS_PROXY -u HTTP_PROXY -u https_proxy -u http_proxy -u ALL_PROXY -u all_proxy`，curl 加 `--noproxy '*'`。git over SSH 正常。
- **镜像预拉**：kind 节点继承 host 代理、容器内拉不到 docker.io/quay.io → host `docker pull` 后 `kind load docker-image ... --name apihub`。
- **MinIO**：`apihub`/`apihub_dev_pwd`；compose 容器 `apihub-minio`。

---

## File Structure

| 文件 | 职责 | task |
|---|---|---|
| `services/services/workflow/src/workflow_svc/argo_client.py` | **改**：`_node_to_step` 修 inputs/outputs list→dict bug + 加 `_params_to_dict` | 1 |
| `services/services/workflow/tests/test_argo_mapping.py` | **新**：`_phase_to_status`/`_node_to_step` 单测（CI） | 1 |
| `services/services/dispatcher/src/dispatcher/routes.py` | **改**：cancel/resume/logs 三个薄代理 | 2 |
| `services/services/dispatcher/tests/test_jobs.py` | **改**：三个代理的单测 | 2 |
| `deploy/k8s/base/argo/argo-exec-rbac.yaml` | **新**：SA `argo-exec` + Role(pods) + RoleBinding（ns apihub-workflow） | 3 |
| `deploy/k8s/overlays/kind/patches/workflow-argo-mode.yaml` | **新**：ConfigMap patch `ARGO_MODE: k8s` | 3 |
| `deploy/k8s/overlays/kind/kustomization.yaml` | **改**：+ resource + patch | 3 |
| `scripts/kind/argo-setup.sh` | **新**：装 Argo + 镜像预拉 + artifactRepository(MinIO) + 自检 | 4 |
| `scripts/init-minio/create-buckets.sh` | **改**：+ `argo-artifacts` bucket | 5 |
| `scripts/kind/bootstrap.sh` | **改**：起 MinIO + 调 argo-setup + restart workflow | 5 |
| `scripts/smoke/k8s-workflow-argo.py` | **新**：headline smoke（succeeded/cancel/resume/logs） | 6 |
| `docs/05-core-flows.md` | **改**：§4 注明 kind 现跑真 Argo + MinIO artifact | 6 |
| `scripts/smoke/k8s-workflow-minio.py` | **新**：MinIO 产物往返 smoke | 7 |

**spec 偏离说明**：spec §4 列了 `shared-infra.yaml` 加 MINIO env 一行 → **删除（YAGNI）**：本轮无 pod 消费 MINIO env（Argo executor 经 `artifactRepository` 配置直连 MinIO，由 argo-setup.sh 写入 argo ns，不经 pod env）。MinIO endpoint 由 argo-setup.sh 自己探测（`docker port` read-back）。

---

## Task 1: argo_client mapper 单测 + 修 inputs/outputs bug

**Files:**
- Create: `services/services/workflow/tests/test_argo_mapping.py`
- Modify: `services/services/workflow/src/workflow_svc/argo_client.py:351-377`（`_node_to_step`）

**Interfaces:**
- Consumes: `workflow_svc.argo_client._phase_to_status(phase:str)->WorkflowStatus`、`_node_to_step(node:dict)->WorkflowStep`（均现存）；`workflow_svc.models.WorkflowStatus`/`StepStatus`。
- Produces: 新增 `_params_to_dict(params: list|None) -> dict[str,str]`；`_node_to_step` 的 inputs/outputs 改为 dict。

**背景**：`_node_to_step`（argo_client.py:375-376）把 Argo 的 `parameters`（**list**）塞进 `WorkflowStep.inputs/outputs`（pydantic 类型 `dict[str,str]`，models.py:47-48）→ k8s 模式构造 WorkflowStep 时 pydantic 校验炸。stub 模式 get_steps 用默认 `{}` 故未暴露。

- [ ] **Step 1: 写失败测试**

Create `services/services/workflow/tests/test_argo_mapping.py`:

```python
"""K8sArgoClient 的 phase/node 映射单测（CI，不依赖 kind）。"""

from workflow_svc.argo_client import _node_to_step, _params_to_dict, _phase_to_status
from workflow_svc.models import StepStatus, WorkflowStatus


def test_phase_to_status_known():
    assert _phase_to_status("Succeeded") is WorkflowStatus.SUCCEEDED
    assert _phase_to_status("Running") is WorkflowStatus.RUNNING
    assert _phase_to_status("Failed") is WorkflowStatus.FAILED
    assert _phase_to_status("Error") is WorkflowStatus.FAILED
    assert _phase_to_status("Stopped") is WorkflowStatus.CANCELLED
    assert _phase_to_status("Skipped") is WorkflowStatus.CANCELLED
    assert _phase_to_status("Pending") is WorkflowStatus.SUBMITTED


def test_phase_to_status_unknown():
    assert _phase_to_status("") is WorkflowStatus.UNKNOWN
    assert _phase_to_status("WeirdPhase") is WorkflowStatus.UNKNOWN


def test_params_to_dict():
    params = [{"name": "a", "value": "1"}, {"name": "b", "value": "x"}]
    assert _params_to_dict(params) == {"a": "1", "b": "x"}
    assert _params_to_dict(None) == {}
    assert _params_to_dict([]) == {}


def test_node_to_step_maps_fields_and_inputs_as_dict():
    node = {
        "name": "wf-abc123[0].s1",
        "phase": "Succeeded",
        "templateName": "echo",
        "startedAt": "2026-07-10T10:00:00Z",
        "finishedAt": "2026-07-10T10:00:05Z",
        "message": "ok",
        "inputs": {"parameters": [{"name": "p", "value": "v"}]},
        "outputs": {"parameters": [{"name": "o", "value": "w"}]},
    }
    step = _node_to_step(node)
    assert step.name == "wf-abc123[0].s1"
    assert step.template == "echo"
    assert step.status is StepStatus.SUCCEEDED
    assert step.started_at is not None and step.started_at.year == 2026
    assert step.finished_at is not None
    assert step.message == "ok"
    # 关键：inputs/outputs 必须是 dict（Argo parameters list → dict），否则 pydantic 炸
    assert step.inputs == {"p": "v"}
    assert step.outputs == {"o": "w"}


def test_node_to_step_missing_fields_safe():
    step = _node_to_step({"name": "n", "phase": "Running"})
    assert step.name == "n"
    assert step.status is StepStatus.RUNNING
    assert step.started_at is None
    assert step.inputs == {}
    assert step.outputs == {}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest services/services/workflow/tests/test_argo_mapping.py -v`
Expected: FAIL —— `_params_to_dict` ImportError（不存在）；且 `test_node_to_step_maps_fields_and_inputs_as_dict` 即使 import 修了也会因现实现把 list 塞 dict 字段而 pydantic 校验失败 / inputs != `{"p":"v"}`。

- [ ] **Step 3: 实现 `_params_to_dict` 并修 `_node_to_step`**

Modify `services/services/workflow/src/workflow_svc/argo_client.py`，在 `_node_to_step` 之前加 helper，并改 `_node_to_step` 的 inputs/outputs 两行：

在 `def _node_to_step(node: dict) -> WorkflowStep:` 之前插入：

```python
def _params_to_dict(params: list | None) -> dict[str, str]:
    """Argo parameters [{name, value}, ...] → {name: value}。

    WorkflowStep.inputs/outputs 类型是 dict[str,str]；直接塞 Argo 的 list
    会触发 pydantic 校验错误（k8s 模式 get_steps 必经路径）。
    """
    return {p.get("name", ""): str(p.get("value", "")) for p in (params or [])}
```

把 `_node_to_step` 末尾 return 的最后两行：

```python
        inputs=node.get("inputs", {}).get("parameters", []),
        outputs=node.get("outputs", {}).get("parameters", []),
```

改成：

```python
        inputs=_params_to_dict(node.get("inputs", {}).get("parameters")),
        outputs=_params_to_dict(node.get("outputs", {}).get("parameters")),
```

- [ ] **Step 4: 跑测试确认通过 + 回归现有 argo 测试**

Run: `pytest services/services/workflow/tests/test_argo_mapping.py services/services/workflow/tests/test_argo_client.py services/services/workflow/tests/test_routes.py -v`
Expected: 全 PASS（含既有 stub/routes 测试不回归）。

- [ ] **Step 5: lint**

Run: `uvx --with ruff==0.6.9 ruff check services/services/workflow/ && uvx --with ruff==0.6.9 ruff format --check services/services/workflow/`
（代理挂 → `.venv-t1/bin/ruff check ...` && `.venv-t1/bin/ruff format --check ...`）
Expected: 无错。

- [ ] **Step 6: Commit**

```bash
git add services/services/workflow/src/workflow_svc/argo_client.py services/services/workflow/tests/test_argo_mapping.py
git commit -m "fix(workflow): _node_to_step inputs/outputs list→dict + mapper 单测"
```

---

## Task 2: dispatcher cancel/resume/logs 薄代理

**Files:**
- Modify: `services/services/dispatcher/src/dispatcher/routes.py:13`（import）+ 末尾加 3 路由
- Test: `services/services/dispatcher/tests/test_jobs.py`（加 3 测试）

**Interfaces:**
- Consumes: `_wf_client(request)`（routes.py:47）、`get_settings().workflow_service_url`（默认 `http://workflow.apihub-system`）、`ApiError`/`ErrorCode`/`JSONResponse`（已 import）。
- Produces: `POST /v1/jobs/{job_id}/cancel`、`POST /v1/jobs/{job_id}/resume`、`GET /v1/jobs/{job_id}/logs`（透传 X-API-Key；logs 透传 SSE body，`media_type=text/event-stream`）。

- [ ] **Step 1: 写失败测试**

Append to `services/services/dispatcher/tests/test_jobs.py`:

```python
async def test_cancel_proxies_to_workflow(async_client):
    """POST /v1/jobs/{id}/cancel → workflow-svc POST /v1/workflows/{id}/cancel。"""
    captured = {}

    class _FakeWF:
        async def post(self, url, json=None, headers=None, timeout=None):  # noqa: ASYNC109
            captured["url"] = url
            captured["headers"] = headers or {}

            class _R:
                status_code = 200

                def json(self_inner):
                    return {"workflow_id": 42, "status": "cancelled"}

                def raise_for_status(self_inner):
                    pass

            return _R()

    async_client.app.state.workflow_client = _FakeWF()
    resp = await async_client.post(
        "/v1/jobs/42/cancel", headers={"X-API-Key": "ak_test_a_demo001"}
    )
    assert resp.status_code == 200, resp.text
    assert resp.json()["status"] == "cancelled"
    assert captured["url"].endswith("/v1/workflows/42/cancel"), captured["url"]
    assert captured["headers"]["X-API-Key"] == "ak_test_a_demo001"


async def test_resume_proxies_to_workflow(async_client):
    """POST /v1/jobs/{id}/resume → workflow-svc POST /v1/workflows/{id}/resume。"""

    class _FakeWF:
        async def post(self, url, json=None, headers=None, timeout=None):  # noqa: ASYNC109
            assert url.endswith("/v1/workflows/42/resume"), url

            class _R:
                status_code = 200

                def json(self_inner):
                    return {"workflow_id": 42, "status": "running"}

                def raise_for_status(self_inner):
                    pass

            return _R()

    async_client.app.state.workflow_client = _FakeWF()
    resp = await async_client.post(
        "/v1/jobs/42/resume", headers={"X-API-Key": "ak_test_a_demo001"}
    )
    assert resp.status_code == 200, resp.text
    assert resp.json()["status"] == "running"


async def test_logs_proxies_sse(async_client):
    """GET /v1/jobs/{id}/logs → workflow-svc GET /v1/workflows/{id}/logs，SSE body 透传。"""
    captured = {}

    class _FakeWF:
        async def get(self, url, headers=None, params=None, timeout=None):  # noqa: ASYNC109
            captured["url"] = url
            captured["params"] = params

            class _R:
                status_code = 200
                content = b'data: {"line":"hi from argo"}\n\n'

                def raise_for_status(self_inner):
                    pass

            return _R()

    async_client.app.state.workflow_client = _FakeWF()
    resp = await async_client.get(
        "/v1/jobs/42/logs?step_name=s1", headers={"X-API-Key": "ak_test_a_demo001"}
    )
    assert resp.status_code == 200, resp.text
    assert resp.headers["content-type"].startswith("text/event-stream")
    assert b"hi from argo" in resp.content
    assert captured["url"].endswith("/v1/workflows/42/logs"), captured["url"]
    assert captured["params"] == {"step_name": "s1"}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest services/services/dispatcher/tests/test_jobs.py -v`
Expected: 三个新测试 FAIL（404 路由不存在）。

- [ ] **Step 3: 加 import + 三路由**

Modify `services/services/dispatcher/src/dispatcher/routes.py:13`：

把
```python
from fastapi.responses import JSONResponse
```
改成
```python
from fastapi.responses import JSONResponse, Response
```

在 `register_routes` 内、`get_job`（`@app.get("/v1/jobs/{job_id}")`）之后追加：

```python
    @app.post("/v1/jobs/{job_id}/cancel")
    async def cancel_job(job_id: int, request: Request):
        """workflow cancel 代理：→ workflow-svc POST /v1/workflows/{id}/cancel。"""
        settings = get_settings()
        client = _wf_client(request)
        resp = await client.post(
            f"{settings.workflow_service_url}/v1/workflows/{job_id}/cancel",
            headers={"X-API-Key": request.headers.get("X-API-Key", "")},
        )
        if resp.status_code == 404:
            raise ApiError(
                ErrorCode.NOT_FOUND,
                f"job {job_id} not found",
                http_status=404,
            )
        if resp.status_code >= 400:
            raise ApiError(
                ErrorCode.INTERNAL,
                f"workflow-svc error: {resp.text[:300]}",
                http_status=502,
            )
        return JSONResponse(status_code=200, content=resp.json())

    @app.post("/v1/jobs/{job_id}/resume")
    async def resume_job(job_id: int, request: Request):
        """workflow resume 代理：→ workflow-svc POST /v1/workflows/{id}/resume。"""
        settings = get_settings()
        client = _wf_client(request)
        resp = await client.post(
            f"{settings.workflow_service_url}/v1/workflows/{job_id}/resume",
            headers={"X-API-Key": request.headers.get("X-API-Key", "")},
        )
        if resp.status_code == 404:
            raise ApiError(
                ErrorCode.NOT_FOUND,
                f"job {job_id} not found",
                http_status=404,
            )
        if resp.status_code >= 400:
            raise ApiError(
                ErrorCode.INTERNAL,
                f"workflow-svc error: {resp.text[:300]}",
                http_status=502,
            )
        return JSONResponse(status_code=200, content=resp.json())

    @app.get("/v1/jobs/{job_id}/logs")
    async def stream_job_logs(job_id: int, request: Request, step_name: str | None = None):
        """workflow logs(SSE) 代理：→ workflow-svc GET /v1/workflows/{id}/logs。

        Arg 已完成 wf 的日志 Argo 一次性返回，故用缓冲式透传（非长连真流式）。
        真 SSE 长流式见 spec §10（后续）。
        """
        settings = get_settings()
        client = _wf_client(request)
        params = {"step_name": step_name} if step_name else None
        resp = await client.get(
            f"{settings.workflow_service_url}/v1/workflows/{job_id}/logs",
            headers={"X-API-Key": request.headers.get("X-API-Key", "")},
            params=params,
        )
        if resp.status_code == 404:
            raise ApiError(
                ErrorCode.NOT_FOUND,
                f"job {job_id} not found",
                http_status=404,
            )
        if resp.status_code >= 400:
            raise ApiError(
                ErrorCode.INTERNAL,
                f"workflow-svc error: {resp.text[:300]}",
                http_status=502,
            )
        return Response(content=resp.content, media_type="text/event-stream")
```

- [ ] **Step 4: 跑测试确认通过 + 回归**

Run: `pytest services/services/dispatcher/tests/ -v`
Expected: 全 PASS（含既有 post/get/422 测试）。

- [ ] **Step 5: lint**

Run: `uvx --with ruff==0.6.9 ruff check services/services/dispatcher/ && uvx --with ruff==0.6.9 ruff format --check services/services/dispatcher/`
（代理挂 → `.venv-t1/bin/ruff ...`）
Expected: 无错。

- [ ] **Step 6: Commit**

```bash
git add services/services/dispatcher/src/dispatcher/routes.py services/services/dispatcher/tests/test_jobs.py
git commit -m "feat(dispatcher): 补 workflow cancel/resume/logs(SSE) 薄代理"
```

---

## Task 3: k8s manifests —— argo-exec RBAC + argo_mode patch + kustomization

**Files:**
- Create: `deploy/k8s/base/argo/argo-exec-rbac.yaml`
- Create: `deploy/k8s/overlays/kind/patches/workflow-argo-mode.yaml`
- Modify: `deploy/k8s/overlays/kind/kustomization.yaml`（resources + patches）

**Interfaces:**
- Consumes: `apihub-workflow` ns（已存在于 `base/namespaces/namespaces.yaml:33`）；ConfigMap `workflow-config`（`services/workflow/configmap.yaml`，现 `ARGO_MODE: stub`）。
- Produces: SA `argo-exec`（ns apihub-workflow，smoke spec 的 `spec.serviceAccountName`）；kind overlay 渲染出 `ARGO_MODE: k8s`。

- [ ] **Step 1: 建 argo-exec RBAC**

Create `deploy/k8s/base/argo/argo-exec-rbac.yaml`:

```yaml
# Argo Workflow executor SA —— Argo controller 在 apihub-workflow 起的 step Pod 以此 SA 跑。
# 与 svc 的 `workflow` SA（apihub-system，打 CRD）职责分离：本 SA 只管 step Pod 的 pods 权限。
# 凡跑真 Argo 的环境（kind + 未来 staging/prod）都需要。
apiVersion: v1
kind: ServiceAccount
metadata:
  name: argo-exec
  namespace: apihub-workflow
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: argo-exec
  namespace: apihub-workflow
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: [""]
    resources: ["pods/log"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: argo-exec
  namespace: apihub-workflow
subjects:
  - kind: ServiceAccount
    name: argo-exec
    namespace: apihub-workflow
roleRef:
  kind: Role
  name: argo-exec
  apiGroup: rbac.authorization.k8s.io
```

- [ ] **Step 2: 建 argo_mode patch**

Create `deploy/k8s/overlays/kind/patches/workflow-argo-mode.yaml`（strategic-merge，仅覆盖 ARGO_MODE，保留 configmap 其它 data 键）:

```yaml
# kind/dev：workflow-svc 切真 Argo（base configmap 仍 stub，staging/prod 不受影响）。
apiVersion: v1
kind: ConfigMap
metadata:
  name: workflow-config
  namespace: apihub-system
data:
  ARGO_MODE: k8s
```

- [ ] **Step 3: 改 kustomization**

Modify `deploy/k8s/overlays/kind/kustomization.yaml`：

在 `resources:` 列表末尾（`../../services/workflow/deployment.yaml` 之后）追加一行：

```yaml
  - ../../base/argo/argo-exec-rbac.yaml
```

在 `patches:` 列表（`patches/workflow-envfrom.yaml` 那条之后）追加：

```yaml
  - path: patches/workflow-argo-mode.yaml
    target: { kind: ConfigMap, name: workflow-config }
```

- [ ] **Step 4: kustomize build 校验**

Run: `kustomize build --load-restrictor LoadRestrictionsNone deploy/k8s/overlays/kind > /tmp/kind-render.yaml`
Expected: 退出码 0（无报错）。

Run: `grep -E 'argo-exec|ARGO_MODE' /tmp/kind-render.yaml`
Expected: 命中 `name: argo-exec`（SA/Role/RoleBinding）、`argo-exec`（subjects/roleRef）、`ARGO_MODE: k8s`。

- [ ] **Step 5: apply 到 live kind（已存在集群）+ restart workflow**

```bash
kustomize build --load-restrictor LoadRestrictionsNone deploy/k8s/overlays/kind | kubectl apply -f -
kubectl -n apihub-workflow get sa argo-exec
kubectl -n apihub-system rollout restart deploy/workflow
kubectl -n apihub-system wait deploy/workflow --for=condition=Available --timeout=120s
kubectl -n apihub-system get deploy workflow -o jsonpath='{.spec.template.spec.containers[0].envFrom}' ; echo
```
Expected: `argo-exec` SA 存在；workflow rollout Available；envFrom 含 workflow-config（其 ARGO_MODE 已 k8s）。

- [ ] **Step 6: 验 workflow-svc 以 k8s 模式起（health/ready）**

Argo 此时**还没装**（Task 4），所以 `K8sArgoClient.__init__` 会读 SA token —— in-cluster pod 有自动挂载 token，**不会**炸；但 submit 会失败（CRD 不存在）。本轮只验「以 k8s 模式 init 完成、pod Running」。

```bash
kubectl -n apihub-system get pod -l app.kubernetes.io/name=workflow
kubectl -n apihub-system port-forward deploy/workflow 18010:80 &
sleep 3
curl -sf http://127.0.0.1:18010/health/ready ; echo
kill %1 2>/dev/null || true
```
Expected: pod Running；`/health/ready` 返回 `"argo_client_ready": true`（init_argo_client(mode=k8s) 成功）。

- [ ] **Step 7: Commit**

```bash
git add deploy/k8s/base/argo/argo-exec-rbac.yaml deploy/k8s/overlays/kind/patches/workflow-argo-mode.yaml deploy/k8s/overlays/kind/kustomization.yaml
git commit -m "feat(deploy): argo-exec RBAC + kind overlay ARGO_MODE=k8s"
```

---

## Task 4: argo-setup.sh —— 装 Argo + 镜像预拉 + artifactRepository

**Files:**
- Create: `scripts/kind/argo-setup.sh`

**Interfaces:**
- Consumes: host compose `apihub-minio` 容器（须已起，Task 5 保证；本脚本独立可跑，缺则报错退出）、kind 集群 `kind-apihub`、host 网桥 IP（`docker network inspect bridge`）。
- Produces: `argo` ns 的 Argo CRD + controller（Running）；`workflow-controller-configmap`（自动探测名）含 `artifactRepository.s3`→MinIO；Secret `argo-minio-secret`；busybox 已 kind-load。

**前提**：Task 3 已 apply（`argo-exec` SA 存在）。Task 5 起 MinIO —— 若独立跑本脚本，先确保 `docker ps | grep apihub-minio`。

- [ ] **Step 1: 写 argo-setup.sh**

Create `scripts/kind/argo-setup.sh`:

```bash
#!/usr/bin/env bash
# =============================================================================
# Argo Workflow 安装（kind/dev）：
#   1) curl --noproxy 拉官方 install.yaml（绕 host 代理坑）
#   2) 镜像 host pull → kind load（kind 节点继承 host 代理，容器内拉不到）
#   3) kubectl apply + 等 controller ready
#   4) 配 artifactRepository → MinIO（host 网桥 IP:port）
#   5) 自检
#
# 前提：kind 集群 kind-apihub 在；apihub-minio 容器在跑（bootstrap Task 5）；
#       argo-exec SA 已 apply（deploy overlay Task 3）。
# =============================================================================
set -euo pipefail
export PATH="$HOME/.local/bin:$PATH"

CLUSTER_NAME="${KIND_CLUSTER_NAME:-apihub}"
ARGO_NS=argo
WF_NS=apihub-workflow
MINIO_CONTAINER=apihub-minio
MINIO_USER="${MINIO_USER:-apihub}"
MINIO_PASSWORD="${MINIO_PASSWORD:-apihub_dev_pwd}"
ARGO_BUCKET=argo-artifacts

log() { printf '\n\033[1m== %s ==\033[0m\n' "$*"; }
say() { printf '  %s\n' "$*"; }

# ---------- 0) 探测 host IP + MinIO 端口（kind pod 经此访问 host MinIO）----------
HOST_IP=$(docker network inspect bridge --format '{{(index .IPAM.Config 0).Gateway}}')
MINIO_HP=$(docker port "$MINIO_CONTAINER" 9000 2>/dev/null | awk -F: '/^0\.0.0.0:/ {print $NF; exit}')
if [ -z "${MINIO_HP:-}" ]; then
  echo "FATAL: apihub-minio 未起或未 publish 9000（先跑 bootstrap Task 5 起 MinIO）" >&2
  exit 1
fi
MINIO_EP="http://${HOST_IP}:${MINIO_HP}"
say "host_ip=$HOST_IP minio_endpoint=$MINIO_EP"

# ---------- 1) 拉 install.yaml（绕代理）----------
log "fetch argo-workflows install.yaml (stable)"
INSTALL=/tmp/argo-install.yaml
env -u HTTPS_PROXY -u HTTP_PROXY -u https_proxy -u http_proxy -u ALL_PROXY -u all_proxy \
  curl --noproxy '*' -fsSL \
  https://raw.githubusercontent.com/argoproj/argo-workflows/stable/manifests/install.yaml \
  -o "$INSTALL"
say "fetched $(grep -c '^kind:' "$INSTALL") manifests"

# ---------- 2) 镜像预拉 → kind load ----------
log "preload argo images (host pull → kind load)"
mapfile -t IMAGES < <(grep -oE 'image:[[:space:]]*"[^"]+"' "$INSTALL" \
  | sed -E 's/image:[[:space:]]*"//; s/"$//' | sort -u)
for img in "${IMAGES[@]}"; do
  say "pull+load $img"
  docker image inspect "$img" >/dev/null 2>&1 || docker pull "$img"
  kind load docker-image "$img" --name "$CLUSTER_NAME"
done
# smoke step 镜像
say "pull+load busybox:latest"
docker image inspect busybox:latest >/dev/null 2>&1 || docker pull busybox:latest
kind load docker-image busybox:latest --name "$CLUSTER_NAME"

# ---------- 3) apply + 等 controller ----------
log "kubectl apply argo install.yaml"
kubectl apply -f "$INSTALL"
log "wait workflow-controller Available"
kubectl -n "$ARGO_NS" wait deploy/workflow-controller --for=condition=Available --timeout=300s

# ---------- 4) artifactRepository → MinIO ----------
log "configure artifactRepository → MinIO ($ARGO_BUCKET)"
kubectl -n "$ARGO_NS" create secret generic argo-minio-secret \
  --from-literal=accessKey="$MINIO_USER" --from-literal=secretKey="$MINIO_PASSWORD" \
  -o yaml --dry-run=client | kubectl apply -f -

# 探测 controller 实际读的 ConfigMap 名（版本间命名漂移：configmap vs config-map）
CM_NAME=$(kubectl -n "$ARGO_NS" get deploy workflow-controller \
  -o jsonpath='{.spec.template.spec.containers[0].args}' 2>/dev/null \
  | grep -oE 'configmap=[A-Za-z0-9_-]+' | head -1 | cut -d= -f2)
CM_NAME="${CM_NAME:-workflow-controller-config-map}"
say "controller configmap = $CM_NAME"

cat <<EOF | kubectl -n "$ARGO_NS" apply -f -
apiVersion: v1
kind: ConfigMap
metadata:
  name: ${CM_NAME}
  namespace: ${ARGO_NS}
data:
  config: |
    artifactRepository:
      s3:
        endpoint: "${MINIO_EP}"
        bucket: ${ARGO_BUCKET}
        insecure: true
        accessKeySecret:
          name: argo-minio-secret
          key: accessKey
        secretKeySecret:
          name: argo-minio-secret
          key: secretKey
EOF
# 重启 controller 让其重读 config
kubectl -n "$ARGO_NS" rollout restart deploy/workflow-controller
kubectl -n "$ARGO_NS" rollout status deploy/workflow-controller --timeout=180s

# ---------- 5) 自检 ----------
log "self-check"
kubectl get crd workflows.argoproj.io >/dev/null
kubectl -n "$ARGO_NS" get deploy workflow-controller >/dev/null
kubectl -n "$WF_NS" get sa argo-exec >/dev/null
kubectl -n "$ARGO_NS" get cm "$CM_NAME" >/dev/null
kubectl -n "$ARGO_NS" get secret argo-minio-secret >/dev/null
say "ARGO SETUP OK —— controller ready, argo-exec SA present, artifactRepository→MinIO"
```

Then: `chmod +x scripts/kind/argo-setup.sh`

- [ ] **Step 2: 语法检查**

Run: `bash -n scripts/kind/argo-setup.sh`
Expected: 退出码 0。

- [ ] **Step 3: 起 MinIO（独立增量，带端口 override）+ 跑脚本**

> 本步需要 MinIO 容器在跑。host 9000 多半被占（handoff）→ 用 override 重映射到空闲高位端口 19000（argo-setup 经 `docker port apihub-minio 9000` read-back 自动拿到 19000）。这条增量路径保护现有 18h live kind 集群不被 bootstrap 的 `kind delete` 清掉。

```bash
cat > /tmp/minio-ov.yml <<'EOF'
services:
  minio:
    ports: !override
      - "19000:9000"
EOF
docker compose --env-file .env.dev -f docker-compose.dev.yml -f /tmp/minio-ov.yml up -d minio minio-init
docker port apihub-minio 9000   # 应显示 0.0.0.0:19000
docker exec apihub-minio mc alias set local http://localhost:9000 apihub apihub_dev_pwd >/dev/null 2>&1 || true
```

Run: `bash scripts/kind/argo-setup.sh`
Expected: 末尾打印 `ARGO SETUP OK ...`（`minio_endpoint=http://<HOST_IP>:19000`）。

- [ ] **Step 4: 验证 Argo 真起**

```bash
kubectl get crd workflows.argoproj.io
kubectl -n argo get pods
kubectl -n argo get deploy workflow-controller -o jsonpath='{.status.conditions[?(@.type=="Available")].status}'; echo
```
Expected: CRD 存在；`workflow-controller` pod Running；Available=True。

- [ ] **Step 5: Commit**

```bash
git add scripts/kind/argo-setup.sh
git commit -m "feat(kind): argo-setup.sh 装 Argo + 镜像预拉 + artifactRepository(MinIO)"
```

---

## Task 5: bootstrap.sh 起 MinIO + 调 argo-setup + restart workflow

**Files:**
- Modify: `scripts/init-minio/create-buckets.sh`（+ `argo-artifacts` bucket）
- Modify: `scripts/kind/bootstrap.sh`（MinIO pick_host_port + override + bring-up + read-back；apply 后调 argo-setup.sh + restart workflow）

**Interfaces:**
- Consumes: `pick_host_port`/`read_compose_host_port`（bootstrap.sh 现有 helper）；`argo-setup.sh`（Task 4）。
- Produces: host `apihub-minio` 容器在跑（publish 高位端口，无冲突）；`argo-artifacts` bucket 存在；bootstrap 流程一键把 kind 栈 + 真 Argo + k8s workflow 全起来。

- [ ] **Step 1: 加 argo-artifacts bucket**

Modify `scripts/init-minio/create-buckets.sh`，把 bucket 循环行：

```bash
for bucket in call-bodies sdk-packages audit-archive tfstate; do
```

改成：

```bash
for bucket in call-bodies sdk-packages audit-archive tfstate argo-artifacts; do
```

- [ ] **Step 2: bootstrap 加 MinIO 端口探测**

Modify `scripts/kind/bootstrap.sh`：

在 `PG_HP=$(pick_host_port postgres 5432)` 那一行（约 line 53）之后追加：

```bash
MINIO_HP=$(pick_host_port minio 9000)
```

并把那附近的 echo 行（`echo "host ports: redis=..."`）改成包含 minio：

```bash
echo "host ports: redis=$REDIS_HP (default 6379)  postgres=$PG_HP (default 5432)  minio=$MINIO_HP (default 9000)"
```

- [ ] **Step 3: bootstrap override 加 MinIO 端口重映射**

Modify `scripts/kind/bootstrap.sh` 的 override 生成块（`{ echo "services:"; ... } > "$OVR"`），在 postgres 段之后、闭合 `}` 之前追加 minio 段：

```bash
  echo "  minio:"
  echo "    ports: !override"
  echo "      - \"$MINIO_HP:9000\""
```

（即 override 块变成 redis + postgres + minio 三段。）

- [ ] **Step 4: bootstrap bring-up 加 minio + minio-init**

Modify `scripts/kind/bootstrap.sh` 的 `docker compose ... up -d` 行（约 line 75-76），把：

```bash
docker compose --env-file .env.dev -f docker-compose.dev.yml -f "$OVR" \
  up -d postgres redis kafka kafka-init clickhouse jaeger otel-collector
```

改成（追加 `minio minio-init`）：

```bash
docker compose --env-file .env.dev -f docker-compose.dev.yml -f "$OVR" \
  up -d postgres redis kafka kafka-init clickhouse jaeger otel-collector minio minio-init
```

- [ ] **Step 5: bootstrap read-back 加 MinIO 端口**

Modify `scripts/kind/bootstrap.sh` 的 read-back 段（约 line 123-124，`REDIS_HP=$(read_compose_host_port ...)` 之后）追加：

```bash
MINIO_HP=$(read_compose_host_port apihub-minio 9000)
[ -n "$MINIO_HP" ] || { echo "FATAL: apihub-minio host port read-back empty" >&2; exit 1; }
```

并把那附近 echo 行（`echo "overlay-sync host ports ..."`）改成包含 minio：

```bash
echo "overlay-sync host ports (read back): redis=$REDIS_HP pg=$PG_HP minio=$MINIO_HP  (kafka fixed 9094)"
```

- [ ] **Step 6: bootstrap apply 后调 argo-setup + restart workflow**

Modify `scripts/kind/bootstrap.sh`：在健康抽检段（`curl -sf http://127.0.0.1:18000/health/ready ...` 与最终 `echo "DONE: ..."` 之间）插入：

```bash
# 8) 装 Argo Workflow + 配 MinIO artifactRepository（真 Argo e2e）
echo "=== argo-setup (real Argo) ==="
bash scripts/kind/argo-setup.sh

# workflow pod 须以 argo_mode=k8s 重建（overlay 已把 configmap 改 k8s）
kubectl -n apihub-system rollout restart deploy/workflow
kubectl -n apihub-system wait deploy/workflow --for=condition=Available --timeout=120s
```

并把最终 DONE echo 改成：

```bash
echo "DONE: kind stack up + real Argo. host_ip=$HOST_IP redis=$REDIS_HP pg=$PG_HP minio=$MINIO_HP"
```

- [ ] **Step 7: 语法检查**

Run: `bash -n scripts/kind/bootstrap.sh && bash -n scripts/init-minio/create-buckets.sh`
Expected: 退出码 0。

- [ ] **Step 8: 增量验证（live kind，不重建集群）**

不必整跑 bootstrap（会 `kind delete` 清掉现有 18h 集群）。手动补齐当前栈——MinIO 用 override 重映射到 19000（host 9000 多半被占）：

```bash
# MinIO 起带（override 到 19000；argo-setup 经 docker port read-back 自动拿 19000）
cat > /tmp/minio-ov.yml <<'EOF'
services:
  minio:
    ports: !override
      - "19000:9000"
EOF
docker compose --env-file .env.dev -f docker-compose.dev.yml -f /tmp/minio-ov.yml up -d minio minio-init
docker exec apihub-minio mc alias set local http://localhost:9000 apihub apihub_dev_pwd >/dev/null 2>&1 || true
docker exec apihub-minio mc ls local/argo-artifacts >/dev/null 2>&1 && echo "bucket argo-artifacts OK" || echo "WARN: argo-artifacts bucket 缺，重跑 minio-init"

# 跑 argo-setup（现已可拿到 MinIO 端口 19000）
bash scripts/kind/argo-setup.sh

# workflow 以 k8s 模式重建
kubectl -n apihub-system rollout restart deploy/workflow
kubectl -n apihub-system wait deploy/workflow --for=condition=Available --timeout=120s
```
Expected: `argo-setup.sh` 打印 `ARGO SETUP OK`；workflow Available。

> 注：本步与 Task 4 Step 3 共用同一条 MinIO 起带法。两步只跑其一即可（若 Task 4 已起过 MinIO + 跑过 argo-setup，本步只需 restart workflow）。

- [ ] **Step 9: Commit**

```bash
git add scripts/init-minio/create-buckets.sh scripts/kind/bootstrap.sh
git commit -m "feat(kind): bootstrap 起 MinIO + argo-artifacts bucket + 调 argo-setup + workflow k8s 重建"
```

---

## Task 6: k8s-workflow-argo.py smoke + docs note

**Files:**
- Create: `scripts/smoke/k8s-workflow-argo.py`
- Modify: `docs/05-core-flows.md`（§4 加真 Argo + MinIO 注记）

**Interfaces:**
- Consumes: APISIX `:30080`（key `ak_test_a_demo001`）；dispatcher `/v1/jobs[/{id}|/{id}/cancel|/{id}/resume|/{id}/logs]`（Task 2）；workflow-svc 真 Argo（Task 4/5）；`argo-exec` SA（Task 3）；seed `smoke-wf-api`（复用 stub smoke seed）。
- Produces: 退出码 0（全断言过）/ 1（assert fail）/ 2（env unavailable）。

**前提**：Task 1–5 全完成（真 Argo 起、workflow k8s 模式、dispatcher 代理、argo-exec SA、busybox 预拉）。

- [ ] **Step 1: 写 smoke**

Create `scripts/smoke/k8s-workflow-argo.py`:

```python
#!/usr/bin/env python3
"""真 Argo CRD e2e smoke（kind）。

经 APISIX → dispatcher /v1/jobs → workflow-svc（真 Argo）：
  A) succeeded 主路径：2-step busybox echo → 轮询到 succeeded，
     断言真 phase 转换（观测过 running）+ steps 来自 Argo nodes。
  B) cancel：sleep 300 长 wf → running → POST cancel → 轮询到 cancelled。
  C) resume（软断言）：suspend wf → POST resume → 200 不报错。
  D) logs(SSE)：succeeded wf → GET logs → body 含 busybox 真输出。

退出码：0 OK / 1 assert fail / 2 env unavailable。
"""

import json
import sys
import time
import urllib.error
import urllib.request

APISIX_URL = "http://127.0.0.1:30080"
DEMO_KEY = "ak_test_a_demo001"
TENANT_ID = "tenant_a"
WF_API_ID = "smoke-wf-api"
POLL_TIMEOUT = 150  # Argo 起 pod + 拉镜像，留足
POLL_INTERVAL = 3


def http(method, url, headers=None, data=None, timeout=30):
    req = urllib.request.Request(url, method=method, data=data, headers=headers or {})
    try:
        with urllib.request.urlopen(req, timeout=timeout) as r:
            return r.status, r.read()
    except urllib.error.HTTPError as e:
        return e.code, e.read()


def sh(cmd):
    import subprocess

    return subprocess.run(cmd, shell=True, capture_output=True, text=True, check=True).stdout


def seed_wf_api():
    sql = f"""
    INSERT INTO api (id, tenant_id, name, description, category, base_path, tags, status, visibility)
    VALUES ('{WF_API_ID}', '{TENANT_ID}', 'smoke wf argo', 'real argo e2e', 'smoke',
            '/smoke-wf-argo', ARRAY['smoke'], 'published', 'tenant')
    ON CONFLICT (id) DO NOTHING;
    """
    with open("/tmp/_wf_argo_seed.sql", "w") as f:
        f.write(sql)
    sh("docker exec -i apihub-pg psql -U apihub -d apihub -v ON_ERROR_STOP=1 < /tmp/_wf_argo_seed.sql")


def submit(spec):
    body = {"api_id": WF_API_ID, "app_id": "app_trading", "spec": spec}
    st, raw = http(
        "POST",
        f"{APISIX_URL}/v1/jobs",
        headers={"X-API-Key": DEMO_KEY, "Content-Type": "application/json"},
        data=json.dumps(body).encode(),
    )
    print(f"  POST /v1/jobs -> HTTP {st} {raw[:200]!r}")
    assert st == 201, f"POST /v1/jobs HTTP {st}: {raw}"
    wf = json.loads(raw)
    assert "id" in wf and "status" in wf, wf
    return wf["id"]


def poll(wf_id, want_statuses, timeout=POLL_TIMEOUT):
    """轮询到 status ∈ want_statuses；返回 (final_status, seen_statuses_set, last_detail)。"""
    seen = set()
    deadline = time.time() + timeout
    detail = {}
    while time.time() < deadline:
        st, raw = http("GET", f"{APISIX_URL}/v1/jobs/{wf_id}", headers={"X-API-Key": DEMO_KEY})
        assert st == 200, f"GET /v1/jobs/{wf_id} HTTP {st}: {raw}"
        detail = json.loads(raw)
        status = detail.get("status")
        if status:
            seen.add(status)
        if status in want_statuses:
            return status, seen, detail
        time.sleep(POLL_INTERVAL)
    raise AssertionError(
        f"poll timeout: wf={wf_id} want={want_statuses} seen={seen} last={detail}"
    )


def main():
    print("== seed tenant_a workflow api ==")
    seed_wf_api()

    # ---- A) succeeded 主路径 ----
    print("== A) submit real Argo wf (busybox echo, SA=argo-exec) ==")
    spec_a = {
        "serviceAccountName": "argo-exec",
        "entrypoint": "main",
        "templates": [
            {
                "name": "main",
                "steps": [[{"name": "s1", "template": "echo"}], [{"name": "s2", "template": "echo"}]],
            },
            {
                "name": "echo",
                "container": {"image": "busybox:latest", "command": ["sh", "-c", "echo hi from argo"]},
            },
        ],
    }
    wf_a = submit(spec_a)
    status, seen, detail = poll(wf_a, {"succeeded", "failed"})
    print(f"  final status={status} seen={sorted(seen)} steps={len(detail.get('steps') or [])}")
    assert status == "succeeded", f"A) 未成功: status={status} detail={detail}"
    # 真转换：观测过 running（区别 stub 瞬时）；首轮即 succeeded 也接受
    if seen != {"succeeded"}:
        assert "running" in seen, f"A) 未观测到 running 真转换: seen={seen}"
    # steps 真实：非空且含 succeeded node
    steps = detail.get("steps") or []
    assert len(steps) >= 1, f"A) steps 空: {detail}"
    assert any(s.get("status") == "succeeded" for s in steps), f"A) 无 succeeded step: {steps}"
    print(f"WORKFLOW-A OK —— wf={wf_a} succeeded, {len(steps)} real Argo nodes")

    # ---- B) cancel ----
    print("== B) submit long wf (sleep 300) → cancel ==")
    spec_b = {
        "serviceAccountName": "argo-exec",
        "entrypoint": "main",
        "templates": [
            {"name": "main", "steps": [[{"name": "long", "template": "sleep"}]]},
            {"name": "sleep", "container": {"image": "busybox:latest", "command": ["sleep", "300"]}},
        ],
    }
    wf_b = submit(spec_b)
    poll(wf_b, {"running", "succeeded"}, timeout=60)
    st, raw = http("POST", f"{APISIX_URL}/v1/jobs/{wf_b}/cancel", headers={"X-API-Key": DEMO_KEY})
    print(f"  POST /v1/jobs/{wf_b}/cancel -> HTTP {st} {raw[:120]!r}")
    assert st == 200, f"B) cancel HTTP {st}: {raw}"
    status_b, _, _ = poll(wf_b, {"cancelled", "failed", "succeeded"})
    print(f"  cancel final status={status_b}")
    # Argo Stop 映射 cancelled；容忍已先 succeeded（极端快）
    assert status_b in {"cancelled", "succeeded"}, f"B) cancel 后状态异常: {status_b}"
    print(f"WORKFLOW-B OK —— wf={wf_b} cancel→{status_b}")

    # ---- C) resume（软断言）----
    print("== C) submit suspend wf → resume（软断言）==")
    spec_c = {
        "serviceAccountName": "argo-exec",
        "entrypoint": "main",
        "templates": [
            {"name": "main", "steps": [[{"name": "gate", "template": "hold"}], [{"name": "fin", "template": "echo2"}]]},
            {"name": "hold", "suspend": {}},
            {"name": "echo2", "container": {"image": "busybox:latest", "command": ["sh", "-c", "echo resumed"]}},
        ],
    }
    wf_c = submit(spec_c)
    poll(wf_c, {"running", "succeeded"}, timeout=60)
    st, raw = http("POST", f"{APISIX_URL}/v1/jobs/{wf_c}/resume", headers={"X-API-Key": DEMO_KEY})
    print(f"  POST /v1/jobs/{wf_c}/resume -> HTTP {st} {raw[:120]!r}")
    assert st == 200, f"C) resume HTTP {st}: {raw}"
    status_c, _, _ = poll(wf_c, {"succeeded", "failed"})
    print(f"  resume final status={status_c}")
    # 软断言：resume 200 + wf 走向 succeeded（suspend 解除后 echo2 跑完）
    assert status_c == "succeeded", f"C) resume 后未 succeeded（软断言放宽可改 warning）: {status_c}"
    print(f"WORKFLOW-C OK —— wf={wf_c} resume→{status_c}")

    # ---- D) logs(SSE) ----
    print("== D) GET logs for succeeded wf A ==")
    st, raw = http("GET", f"{APISIX_URL}/v1/jobs/{wf_a}/logs", headers={"X-API-Key": DEMO_KEY}, timeout=30)
    print(f"  GET /v1/jobs/{wf_a}/logs -> HTTP {st} {raw[:200]!r}")
    assert st == 200, f"D) logs HTTP {st}: {raw}"
    assert b"hi from argo" in raw, f"D) logs 不含 busybox 真输出: {raw!r}"
    print(f"WORKFLOW-D OK —— logs 含 'hi from argo'")

    print("ALL OK —— real Argo e2e green")
    sys.exit(0)


if __name__ == "__main__":
    try:
        main()
    except AssertionError as e:
        print(f"SMOKE FAIL: {e}")
        sys.exit(1)
    except (OSError, RuntimeError) as e:
        print(f"SMOKE ENV-UNAVAILABLE: {e}")
        sys.exit(2)
```

- [ ] **Step 2: 跑 smoke**

Run: `python3 scripts/smoke/k8s-workflow-argo.py`
Expected: 退出码 0，末行 `ALL OK —— real Argo e2e green`。

> 排错锚点：
> - POST 502 「no route」/ 404 → Argo CRD 没装（Task 4 没跑）。
> - wf → `failed`「pods is forbidden」→ `argo-exec` SA/Role 没就位（Task 3）。
> - wf → `failed` ImagePull → busybox 没 kind-load（Task 4 step 2）。
> - status 卡 `unknown`/502 → workflow-svc 仍 stub（Task 3/5 没重启 workflow）。

- [ ] **Step 3: docs 注记**

Modify `docs/05-core-flows.md` §4（workflow 时序段），在合适位置加一段（若无 §4 标题则加在 workflow 相关段末）：

```markdown
> **kind/dev 现跑真 Argo**：`argo_mode=k8s`（kind overlay patch），workflow-svc 经 in-cluster SA token 打 `argoproj.io/v1alpha1` Workflow CRD；step Pod 以 `argo-exec` SA（ns `apihub-workflow`）跑。产物经 Argo 原生 `artifactRepository` → MinIO（`argo-artifacts` bucket）。详见 `scripts/kind/argo-setup.sh`、`scripts/smoke/k8s-workflow-argo.py`。
```

- [ ] **Step 4: Commit**

```bash
git add scripts/smoke/k8s-workflow-argo.py docs/05-core-flows.md
git commit -m "test(smoke): 真 Argo CRD e2e（succeeded/cancel/resume/logs）+ docs 注记"
```

---

## Task 7: k8s-workflow-minio.py smoke（MinIO 产物往返）

**Files:**
- Create: `scripts/smoke/k8s-workflow-minio.py`

**Interfaces:**
- Consumes: APISIX `/v1/jobs`；Argo `artifactRepository`→MinIO（Task 4）；`argo-artifacts` bucket（Task 5）；`argo-exec` SA（Task 3）。
- Produces: 退出码 0/1/2；断言 Argo `outputs.artifacts` 经 MinIO 中转后被 `inputs.artifacts` 步骤读到一致内容。

**前提**：Task 1–6 完成；artifactRepository 配置生效（controller 已 restart 读 config）。

- [ ] **Step 1: 写 smoke**

Create `scripts/smoke/k8s-workflow-minio.py`:

```python
#!/usr/bin/env python3
"""MinIO 产物往返 smoke（kind，真 Argo）。

Argo 原生 artifact：step produce 写 /tmp/out.txt 并 outputs.artifacts →
Argo executor 经 artifactRepository(MinIO) PUT；step consume inputs.artifacts
接收并校验内容一致。

退出码：0 OK / 1 assert fail / 2 env unavailable。
"""

import json
import sys
import time
import urllib.error
import urllib.request

APISIX_URL = "http://127.0.0.1:30080"
DEMO_KEY = "ak_test_a_demo001"
TENANT_ID = "tenant_a"
WF_API_ID = "smoke-wf-api"
POLL_TIMEOUT = 150
POLL_INTERVAL = 3


def http(method, url, headers=None, data=None, timeout=30):
    req = urllib.request.Request(url, method=method, data=data, headers=headers or {})
    try:
        with urllib.request.urlopen(req, timeout=timeout) as r:
            return r.status, r.read()
    except urllib.error.HTTPError as e:
        return e.code, e.read()


def sh(cmd):
    import subprocess

    return subprocess.run(cmd, shell=True, capture_output=True, text=True, check=True).stdout


def seed_wf_api():
    sql = f"""
    INSERT INTO api (id, tenant_id, name, description, category, base_path, tags, status, visibility)
    VALUES ('{WF_API_ID}', '{TENANT_ID}', 'smoke wf argo', 'real argo e2e', 'smoke',
            '/smoke-wf-argo', ARRAY['smoke'], 'published', 'tenant')
    ON CONFLICT (id) DO NOTHING;
    """
    with open("/tmp/_wf_argo_seed_minio.sql", "w") as f:
        f.write(sql)
    sh("docker exec -i apihub-pg psql -U apihub -d apihub -v ON_ERROR_STOP=1 < /tmp/_wf_argo_seed_minio.sql")


def poll(wf_id, want, timeout=POLL_TIMEOUT):
    deadline = time.time() + timeout
    detail = {}
    while time.time() < deadline:
        st, raw = http("GET", f"{APISIX_URL}/v1/jobs/{wf_id}", headers={"X-API-Key": DEMO_KEY})
        assert st == 200, f"GET HTTP {st}: {raw}"
        detail = json.loads(raw)
        if detail.get("status") in want:
            return detail["status"], detail
        time.sleep(POLL_INTERVAL)
    raise AssertionError(f"poll timeout wf={wf_id} want={want} last={detail}")


def main():
    print("== seed tenant_a workflow api ==")
    seed_wf_api()

    spec = {
        "serviceAccountName": "argo-exec",
        "entrypoint": "main",
        "templates": [
            {
                "name": "main",
                "steps": [
                    [{"name": "produce", "template": "produce"}],
                    [{"name": "consume", "template": "consume"}],
                ],
            },
            {
                "name": "produce",
                "container": {
                    "image": "busybox:latest",
                    "command": ["sh", "-c", "echo -n artifact-content-xyz > /tmp/out.txt"],
                },
                "outputs": {"artifacts": [{"name": "out", "path": "/tmp/out.txt"}]},
            },
            {
                "name": "consume",
                "inputs": {"artifacts": [{"name": "out", "path": "/tmp/in.txt"}]},
                "container": {
                    "image": "busybox:latest",
                    # 读入 artifact，内容不符则 exit 1 → Argo 标 node Failed → wf failed
                    "command": [
                        "sh", "-c",
                        "cat /tmp/in.txt; echo; "
                        "grep -q artifact-content-xyz /tmp/in.txt || "
                        "(echo MISMATCH && exit 1)",
                    ],
                },
            },
        ],
    }

    body = {"api_id": WF_API_ID, "app_id": "app_trading", "spec": spec}
    st, raw = http(
        "POST", f"{APISIX_URL}/v1/jobs",
        headers={"X-API-Key": DEMO_KEY, "Content-Type": "application/json"},
        data=json.dumps(body).encode(),
    )
    print(f"  POST /v1/jobs -> HTTP {st} {raw[:200]!r}")
    assert st == 201, f"POST HTTP {st}: {raw}"
    wf_id = json.loads(raw)["id"]

    status, detail = poll(wf_id, {"succeeded", "failed"})
    print(f"  final status={status} steps={len(detail.get('steps') or [])}")
    # consume 校验失败会变 failed —— succeeded 即证明产物经 MinIO 往返一致
    assert status == "succeeded", f"MinIO artifact 往返失败（consume 校验不过 / Argo artifact error）: {detail}"

    # 软断言：直查 MinIO bucket 存在对象（Argo 产物 key 随机，只验 bucket 非空）
    try:
        out = sh(
            "docker exec apihub-minio mc ls --recursive local/argo-artifacts/ 2>/dev/null | head"
        )
        print(f"  [soft] minio argo-artifacts listing:\n{out or '  (空或 mc 未配)'}")
    except Exception as e:  # noqa: BLE001
        print(f"  [soft] minio 直查跳过: {e}")

    print(f"WORKFLOW-MINIO OK —— wf={wf_id} succeeded, artifact 经 MinIO 往返一致")
    sys.exit(0)


if __name__ == "__main__":
    try:
        main()
    except AssertionError as e:
        print(f"SMOKE FAIL: {e}")
        sys.exit(1)
    except (OSError, RuntimeError) as e:
        print(f"SMOKE ENV-UNAVAILABLE: {e}")
        sys.exit(2)
```

- [ ] **Step 2: 跑 smoke**

Run: `python3 scripts/smoke/k8s-workflow-minio.py`
Expected: 退出码 0，末行 `WORKFLOW-MINIO OK ...`。

> 排错锚点（产物失败常见）：
> - wf `failed`、consume step 报 artifact 找不到 → artifactRepository 未生效（Task 4 controller 没 restart / ConfigMap 名探测错）。
> - wf `failed`「EndpointConnectionError」→ MinIO 端口/endpoint 错（host IP 或 read-back 端口不对）。
> - wf `failed` S3 虚拟主机/path-style → MinIO path-style：artifactRepository.s3 已用显式 endpoint + bucket，默认 path-style；若仍报，加 `useBucketAsHost: false`（多数版本默认即此）。
> - wf `error`「access denied」→ `argo-minio-secret` 凭证错（`apihub`/`apihub_dev_pwd`）。

- [ ] **Step 3: Commit**

```bash
git add scripts/smoke/k8s-workflow-minio.py
git commit -m "test(smoke): MinIO 产物往返（Argo 原生 artifact）"
```

---

## 收尾

- [ ] 跑全套 smoke：`python3 scripts/smoke/k8s-workflow-argo.py` + `python3 scripts/smoke/k8s-workflow-minio.py`（stub smoke `k8s-workflow.py` 与真 Argo 不能同栈并存——stub 需 `argo_mode=stub`，本轮栈是 k8s，不回跑）。
- [ ] CI 单测：`pytest services/services/workflow/tests/ services/services/dispatcher/tests/ -v`。
- [ ] lint 全量：`uvx --with ruff==0.6.9 ruff check . && uvx --with ruff==0.6.9 ruff format --check .`（代理挂用 `.venv-t1/bin/ruff`）。
- [ ] 推分支 + 开 PR（开 PR/查 CI 加 `env -u ..._PROXY`，见 Global Constraints）。
- [ ] 更新交接文档 `docs/superpowers/handoff-2026-07-10-argo-crd-e2e.md`（标完成 + 下轮种子）。
