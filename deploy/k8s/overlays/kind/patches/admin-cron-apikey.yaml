apiVersion: v1
kind: Secret
metadata:
  name: admin-cron-apikey
  namespace: apihub-system
stringData:
  # kind/dev：用 seed 的 tenant_a admin key（cron 专用 admin app + key 尚未建，见 follow-up）。
  # 走通挂载 + 传输链路即可；admin 鉴权返回 401 也证明 mount fix 本身生效。
  api-key: "ak_test_a_demo001"
